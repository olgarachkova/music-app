import React from 'react';
import { withLayout } from '../../Layout/Layout';

const Create = (): JSX.Element => {
    return (
    <div>tracks create page</div>
    );
}

export default withLayout(Create);